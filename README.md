# Repo testing NIM Lang

Commonly used commands and switches have abbreviations, so you can also use:

```sh
    nim c -r greetings.nim
```

To compile a release version use:

```sh
    nim c -d:release greetings.nim
```
